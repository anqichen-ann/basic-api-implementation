spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rsSystem?useSSL=false
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: create
